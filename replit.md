# Realm of Eternity

Svelte 5 + Vite idle game.

## Setup
- Frontend: Vite dev server on port 5000 (host 0.0.0.0, allowedHosts: true)
- Workflow: `Dev Server` runs `npm run dev`
- Deployment: static, build `npm run build`, publicDir `dist`
